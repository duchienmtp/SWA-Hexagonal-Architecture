eureka:
  client:
    service-url:
      defaultZone: http://discovery-service:8761/eureka
name:
  value: kagami
spring:
  cloud:
    config:
      override-system-properties: false

management:
  tracing:
    sampling:
      probability: 1.0